/**
 * @flow
 *
 * The Icon component wraps the `react-native-vector-icons` Icon component to allow you to easily
 * replace this with an alternative Icon implementation if required
 */
import Icon from 'react-native-vector-icons/Ionicons';

export default Icon;
